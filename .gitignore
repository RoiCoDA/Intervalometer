# Dependencies
node_modules/
.pnp
.pnp.js

# Production / Build
.expo/
dist/
web-build/

# System Files
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ANDROID SPECIFIC (Important for you)
# ---------------------------------
# Build outputs (Binaries, intermediate files)
android/build/
android/app/build/
android/.gradle/

# Local configuration (SDK location changes per machine)
android/local.properties

# Android Studio project files (User specific)
android/*.iml
android/.idea/
android/captures/

# Generated binaries (Don't commit the APKs you build!)
*.apk
*.aab

# SECRETS (Crucial!)
# ---------------------------------
# Block the upload key you just generated
my-upload-key.keystore
*.jks

# Allow the default debug keystore (safe and necessary for dev)
!android/app/debug.keystore